# Generated by scheduled-jobs/gen-website-status.sh — do not edit by hand.
# Redacted snapshot of the machine's scheduled agent jobs (see the generator).
updated: "2026-06-24"
total: 13
shown: 12
jobs:
  - name: "Mail watcher"
    blurb: "Checks a dedicated inbox and turns forwarded notes into tasks."
    cadence: "continuous"
    enabled: true
  - name: "Project improver"
    blurb: "Rotates through side projects making small, safe improvements."
    cadence: "continuous"
    enabled: true
  - name: "Side-project builder"
    blurb: "Incrementally builds out an experimental side project."
    cadence: "continuous"
    enabled: true
  - name: "Status publisher"
    blurb: "Keeps this very page in sync with the live job list."
    cadence: "hourly"
    enabled: true
  - name: "Task proposer"
    blurb: "Drafts small task and cleanup suggestions through the day."
    cadence: "hourly"
    enabled: true
  - name: "Goal planner"
    blurb: "Reads our goals each morning and makes one small move toward them."
    cadence: "daily"
    enabled: true
  - name: "Notes linter"
    blurb: "Keeps the personal knowledge vault tidy and consistent."
    cadence: "daily"
    enabled: true
  - name: "Project scout"
    blurb: "Looks across the projects for the next useful move."
    cadence: "daily"
    enabled: true
  - name: "Skill scout"
    blurb: "Spots repeated work worth turning into a reusable skill."
    cadence: "daily"
    enabled: true
  - name: "Task digest"
    blurb: "Sends a twice-daily summary of what's coming due."
    cadence: "daily"
    enabled: true
  - name: "Expense reminder"
    blurb: "Nudges me to file business expenses."
    cadence: "weekly"
    enabled: false
  - name: "Value auditor"
    blurb: "Asks each week whether these automations still earn their keep."
    cadence: "weekly"
    enabled: true
